{{ form_start(formR, {'attr': {'id': 'form-reponse-unique'}}) }}
    <input type="hidden" name="parent_id" id="parent-id-input" value="">
    {{ form_widget(formR) }}
    <button class="btn btn-primary">Répondre</button>
{{ form_end(formR) }}