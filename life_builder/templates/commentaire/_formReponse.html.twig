{{ form_start(form) }}
    <input type="hidden" name="commentaire_id" value="{{ commentaire.id }}">
    <input
        name="{{ field_name(form.description) }}"
        type="text"
        value="{{ form.description.vars.value|e }}"
    >
    
    <button class="btn">{{ button_label|default('Save') }}</button>
    <button type="button"
                        class="btn btn-secondary btn-sm"
                        onclick="toggleForm('form-commentaire')">
                    Annuler
    </button>
{{ form_end(form) }}